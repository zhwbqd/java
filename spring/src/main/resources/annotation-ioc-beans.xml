<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                http://www.springframework.org/schema/context
                http://www.springframework.org/schema/context/spring-context-3.0.xsd
                http://www.springframework.org/schema/task
                http://www.springframework.org/schema/task/spring-task.xsd"
                default-autowire="constructor">

	<context:annotation-config />
	<context:component-scan
		base-package="zhwb.spring.ioc.annotation,zhwb.study.spring.prototype" />

	<context:component-scan base-package="zhwb.study.event" />

	<task:executor id="executor" pool-size="10" />
	<bean id="applicationEventMulticaster"
		class="org.springframework.context.event.SimpleApplicationEventMulticaster">
		<!-- 注入任务执行器 这样就实现了异步调用（缺点是全局的，要么全部异步，要么全部同步（删除这个属性即是同步）） -->
		<property name="taskExecutor" ref="executor" />
	</bean>

	<!-- <context:component-scan base-package="zhwb.spring.ioc.annotation"> 
		<context:include-filter type="regex" expression="Notification.*" /> </context:component-scan> -->

</beans>